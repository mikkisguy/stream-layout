const Secondary = ({ asOutro = false }) => {
  /**
   * Default: intro scene with countdown clock
   * asOutro: outro scene with thank yous
   */
  console.log("asOutro", asOutro);
};

export default Secondary;
